{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd discord-bot && npm install && npm start",
    "restartPolicyType": "ALWAYS"
  }
}
